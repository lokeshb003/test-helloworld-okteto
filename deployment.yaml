apiVersion: apps/v1
kind: Deployment
metadata:
  name: updated-portfolio-deployment
spec:
  replicas: 1
  selector:
    matchLabels:
      app: python-app
  template:
    metadata:
      labels:
        app: python-app
    spec:
      containers:
      - name: python-app
        image: ghcr.io/lokeshb003/test-helloworld-okteto:master